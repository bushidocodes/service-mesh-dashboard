import TableHeader from "components/Main/components/TableHeader";
import styled from "styled-components";

const TableStatusHeader = styled(TableHeader)`
  text-transform: capitalize;
  padding-left: 4px;
`;
export default TableStatusHeader;
