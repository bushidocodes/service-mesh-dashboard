import styled from "styled-components";

const Message = styled.div`
  position: absolute;
  top: 50%;
  left: 50%;
  margin-top: -20px;
  margin-left: -150px;
  width: 300px;
  text-align: center;
`;

export default Message;
